{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "app",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      },
      "outputs": [
        "dist"
      ]
    }
  },
  "tags": []
}
